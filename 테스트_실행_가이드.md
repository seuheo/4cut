# KTX 네컷 앱 테스트 실행 가이드

**작성일**: 2025년 1월 14일  
**대상 버전**: MVP v2.0

---

## ✅ 코드 레벨 검증 완료

### 1. 빌드 상태
- ✅ **빌드 성공**: `./gradlew assembleDebug` 완료
- ✅ **컴파일 오류**: 없음
- ✅ **코드 구조**: 정상

### 2. 핵심 기능 구현 확인
- ✅ JSON 프레임 시스템: `loadSlotsFromJson()` 구현 확인
- ✅ 크롭 기능: `CropScreen.kt`, `FrameSlotCalculator.kt` 구현 확인
- ✅ 동영상 생성: `VideoSlideShowCreator.kt` 구현 확인
- ✅ 즉시 저장: `FrameViewModel.saveImage()` 구현 확인

---

## 📱 실제 앱 테스트 실행 방법

### Step 1: 앱 설치 및 실행

```bash
# 디버그 APK 빌드
.\gradlew assembleDebug

# APK 설치 (에뮬레이터/디바이스 연결 후)
adb install app\build\outputs\apk\debug\app-debug.apk

# 앱 실행
adb shell am start -n com.example.a4cut/.MainActivity

# Logcat 모니터링 (별도 터미널)
adb logcat | Select-String -Pattern "com.example.a4cut"
```

### Step 2: 기본 기능 테스트

#### 2.1 권한 테스트
1. 앱 실행
2. 권한 요청 팝업 확인
3. 권한 허용/거부 테스트

#### 2.2 프레임 선택 테스트
1. 하단 네비게이션에서 "Frame" 탭 클릭
2. 프레임 캐러셀 확인
3. 롱 폼 프레임(Long Form Black/White) 선택

#### 2.3 사진 선택 테스트
1. "갤러리에서 사진 선택" 버튼 클릭
2. 4장의 사진 선택
3. 각 슬롯에 이미지가 표시되는지 확인

#### 2.4 크롭 기능 테스트 (롱 폼 프레임)
1. 롱 폼 프레임 선택 후
2. 각 슬롯(1~4번)에 이미지 선택
3. 크롭 화면으로 자동 이동 확인
4. 비율이 자동으로 조정되는지 확인
5. 크롭 완료 후 원래 화면으로 돌아와서 이미지가 표시되는지 확인

#### 2.5 이미지 합성 테스트
1. 4장의 사진 모두 선택
2. "완성하기" 버튼 클릭
3. 미리보기 다이얼로그 확인
4. "저장" 버튼 클릭
5. 갤러리에서 저장된 이미지 확인

#### 2.6 즉시 저장 검증
1. 이미지 저장 직후
2. 홈 화면으로 이동
3. 저장된 사진이 즉시 표시되는지 확인 (약 20ms 이내)
4. 달력 화면에서 사진이 있는 날짜가 하이라이트되는지 확인

#### 2.7 동영상 생성 검증
1. Logcat에서 다음 로그 확인:
   ```
   VideoSlideShowCreator: JCodec 동영상 생성 시작
   VideoSlideShowCreator: X번째 사진 인코딩 중...
   FrameViewModel: 동영상 생성 완료
   ```
2. 사진 상세 화면에서 동영상 공유 버튼 확인

---

## 🧪 단계별 테스트 시나리오

### 시나리오 1: 기본 4컷 사진 생성
1. **프레임 선택**: 기본 프레임 선택
2. **사진 선택**: 갤러리에서 4장 선택
3. **합성**: "완성하기" 버튼 클릭
4. **저장**: "저장" 버튼 클릭
5. **확인**: 갤러리에서 저장된 이미지 확인

**예상 결과**: 
- ✅ 합성된 이미지가 1080x1920 해상도로 저장됨
- ✅ 홈 화면에서 즉시 확인 가능
- ✅ DB에 사진 정보 저장됨

### 시나리오 2: 롱 폼 프레임 + 크롭 기능
1. **프레임 선택**: Long Form Black 선택
2. **1번 슬롯**: 이미지 선택 → 크롭 화면 → 크롭 완료
3. **2번 슬롯**: 이미지 선택 → 크롭 화면 → 크롭 완료
4. **3번 슬롯**: 이미지 선택 → 크롭 화면 → 크롭 완료
5. **4번 슬롯**: 이미지 선택 → 크롭 화면 → 크롭 완료
6. **합성**: "완성하기" 버튼 클릭
7. **확인**: 각 슬롯에 맞는 비율로 이미지가 배치되었는지 확인

**예상 결과**:
- ✅ 각 슬롯의 비율이 자동으로 계산됨
- ✅ 크롭된 이미지가 정확한 비율로 배치됨
- ✅ JSON 슬롯 정보가 사용됨 (로그 확인)

### 시나리오 3: 즉시 저장 + 동영상 생성
1. **사진 저장**: 위 시나리오 1 또는 2 실행
2. **즉시 확인**: 저장 직후 홈 화면에서 사진 확인
3. **동영상 확인**: Logcat에서 동영상 생성 로그 확인
4. **동영상 공유**: 사진 상세 화면에서 동영상 공유 버튼 확인

**예상 결과**:
- ✅ 저장 직후 즉시 홈 화면에 표시 (약 20ms)
- ✅ 백그라운드에서 동영상 생성 시작
- ✅ 동영상 생성 완료 후 DB 업데이트
- ✅ 사진 상세 화면에서 동영상 공유 가능

---

## 📊 Logcat 검증 포인트

### JSON 프레임 시스템 검증
```
FrameRepository: JSON에서 3개의 프레임 슬롯 정보 로드 완료
FrameViewModel: JSON 슬롯 정보 로드 완료, _frames 업데이트: 4개
ImageComposer: JSON 슬롯 정보 사용: 4개
```

### 크롭 기능 검증
```
PhotoSelectionScreen: 크롭 기능 활성화됨
FrameSlotCalculator: 슬롯 비율 계산
CropScreen: 이미지 로드 및 초기 크롭 영역 설정 완료
PhotoSelectionScreen: CropScreen에서 크롭 완료 결과 수신
```

### 즉시 저장 검증
```
FrameViewModel: 즉시 DB 저장 시작... (videoPath = null)
FrameViewModel: PhotoEntity 즉시 저장 완료: photoId=X, videoPath=null
HomeViewModel: 사진 목록 업데이트: X photos
```

### 동영상 생성 검증
```
VideoSlideShowCreator: JCodec 동영상 생성 시작
VideoSlideShowCreator: 1번째 사진 인코딩 중...
FrameViewModel: 동영상 생성 완료
FrameViewModel: PhotoEntity 저장 완료: photoId=X, videoPath=/path/to/video.mp4
```

---

## 🔍 자동 검증 스크립트

### PowerShell 스크립트 (Windows)

```powershell
# 테스트 자동화 스크립트
$testResults = @()

# 1. 빌드 확인
Write-Host "1. 빌드 확인 중..." -ForegroundColor Yellow
.\gradlew assembleDebug --no-daemon 2>&1 | Select-String -Pattern "BUILD"
if ($LASTEXITCODE -eq 0) {
    $testResults += @{Test="빌드"; Status="✅ 성공"}
} else {
    $testResults += @{Test="빌드"; Status="❌ 실패"}
}

# 2. APK 파일 존재 확인
Write-Host "2. APK 파일 확인 중..." -ForegroundColor Yellow
$apkPath = "app\build\outputs\apk\debug\app-debug.apk"
if (Test-Path $apkPath) {
    $apkSize = (Get-Item $apkPath).Length / 1MB
    $testResults += @{Test="APK 생성"; Status="✅ 성공 ($([math]::Round($apkSize, 2)) MB)"}
} else {
    $testResults += @{Test="APK 생성"; Status="❌ 실패"}
}

# 3. 필수 파일 확인
Write-Host "3. 필수 파일 확인 중..." -ForegroundColor Yellow
$requiredFiles = @(
    "app\src\main\assets\frames.json",
    "app\src\main\java\com\example\a4cut\ui\utils\VideoSlideShowCreator.kt",
    "app\src\main\java\com\example\a4cut\ui\screens\CropScreen.kt"
)
foreach ($file in $requiredFiles) {
    if (Test-Path $file) {
        $testResults += @{Test="파일: $(Split-Path $file -Leaf)"; Status="✅ 존재"}
    } else {
        $testResults += @{Test="파일: $(Split-Path $file -Leaf)"; Status="❌ 없음"}
    }
}

# 결과 출력
Write-Host "`n=== 테스트 결과 ===" -ForegroundColor Green
$testResults | Format-Table -AutoSize
```

---

## 📝 테스트 결과 기록

### 빠른 체크리스트

#### 기본 기능
- [ ] 앱 정상 실행
- [ ] 권한 요청 정상
- [ ] 프레임 선택 가능
- [ ] 사진 선택 가능 (4장)
- [ ] 이미지 합성 성공
- [ ] 갤러리 저장 성공
- [ ] 인스타그램 공유 가능

#### MVP v2.0 기능
- [ ] JSON 프레임 로드 확인 (로그)
- [ ] 롱 폼 프레임 크롭 기능 동작
- [ ] 즉시 저장 확인 (저장 직후 홈 화면 표시)
- [ ] 동영상 생성 확인 (로그)

#### 안정성
- [ ] 크래시 없음
- [ ] 메모리 누수 없음
- [ ] 화면 회전 정상

---

## 🚨 문제 발견 시

### 문제 보고 양식
1. **문제 설명**: _______________
2. **발생 화면**: _______________
3. **재현 방법**: _______________
4. **Logcat 로그**: _______________
5. **심각도**: [ ] Critical [ ] High [ ] Medium [ ] Low

### 즉시 확인 필요 항목
- [ ] NullPointerException 발생 여부
- [ ] 네비게이션 경고 발생 여부
- [ ] 크롭 후 이미지 미반영 여부
- [ ] StateFlow combine 크래시 여부

---

**다음 단계**: 위 가이드를 따라 실제 디바이스/에뮬레이터에서 테스트를 진행하세요.

